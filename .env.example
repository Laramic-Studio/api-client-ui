# Production
# REACT_APP_API_URL=https://noidr.dev/api

# Local dev — direct URL (CORS). Preferred over /api proxy.
REACT_APP_API_URL=http://noidr-api.test/api
REACT_APP_API_PROXY_TARGET=http://noidr-api.test

# Or use CRA proxy instead of direct URL:
# REACT_APP_API_URL=/api
