# Production
# VITE_API_URL=https://noidr.dev/api

# Local dev — direct URL (CORS). Preferred over /api proxy.
VITE_API_URL=http://noidr-api.test/api
VITE_API_PROXY_TARGET=http://noidr-api.test

# Or use the Vite dev proxy instead of direct URL:
# VITE_API_URL=/api
